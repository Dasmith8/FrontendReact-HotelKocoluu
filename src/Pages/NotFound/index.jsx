import Layout from "../../components/Layout";
import React from 'react';

const NotFound = () => {
    return (
        <Layout>
            Not Found
        </Layout>
    );
};

export default NotFound;